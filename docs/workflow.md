# Workflow

## Local Developer Workflow
1. `npm install`
2. `npm run dev`
3. Open two browser windows and join the same session
4. Validate queue operations and sync behavior
5. Run `npm test`

## Release Workflow
1. Update version in `package.json`
2. Run tests
3. Push to GitHub
4. Deploy to Render/Railway
